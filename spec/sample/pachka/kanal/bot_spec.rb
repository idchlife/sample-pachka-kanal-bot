# frozen_string_literal: true

RSpec.describe Sample::Pachka::Kanal::Bot do
  it "has a version number" do
    expect(Sample::Pachka::Kanal::Bot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
